Mullvad gateway VM104 (10.10.10.60; private vmbr1).

Current: single-hop ca-tor-wg-002; DAITA and multihop disabled. Quantum Resistance, LAN sharing, Lockdown Mode, and Mullvad DNS remain enabled. CT198-only policy routing uses table 51820 and wg0-mullvad; historic daita namespace names remain for compatibility.

Full guest notes: /etc/zerofs-network/README.md
Host runbook: /root/CT198-MULLVAD-ROUTING.md

RELATIONSHIP TO MQVPN
- VM104 is not on the inbound mqvpn path.
- CT 212 (`mqvpn-server`, 10.10.10.212) reaches ZeroFS CT 198 (10.10.10.55) directly across vmbr1.
- The tunnel-only ZeroFS alias 10.77.77.55:8080 is DNATed in CT 212 directly to CT 198. It does not traverse this VM.
- Do not policy-route CT 212 or the mqvpn tunnel subnet through VM104; doing so adds an unnecessary VPN layer and can break the deterministic return path.
- VM104 continues to serve CT198-originated remote upload/download egress only, with its existing fail-closed policy and normal-WAN fallback design.

Do not store Mullvad, Tailscale, mqvpn, or relay keys in Proxmox Notes.

Updated 2026-08-24 to document separation from mqvpn ingress.
